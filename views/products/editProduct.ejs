<%- include('../partials/head') %>
    <body>
        <%- include('../partials/header') %>

        <main class="formulario-registro">
            <section class="titulo-registro">
                <h1>Editar Libro <%= itemToEdit.id %> </h1>
            </section>
            <div class="register-form">
                <form class="form-seccion"   action="/products/edit/<%= itemToEdit.id %>?_method=PUT" method="post">
                    <div class="primera-seccion">
                        <div class="input">
                            <h3 class="subtitulo-registro">Información del Libro</h3>
                        </div>
                        <div class="input">
                            <label for="titulo">Titulo:</label>
                            <input type="text" name="titulo" id="titulo" placeholder="titulo" value="<%= itemToEdit.titulo %>" required>                            
                        </div>
                        <div class="input">
                            <label for="descripcion">Descripción:</label>
                            <textarea name="descripcion" id="descripcion" cols="30" rows="10" placeholder="descripcion"><%= itemToEdit.descripcion %> </textarea>
                        </div>
                        <!-- <div class="input image">
                            <label for="portada" class="form-titles">Seleccioná una Imagen de Portada</label>
                            <input type="file" name="portada" id="portada" accept=".png, .jpg" required>
                        </div> -->
                        <div class="input">
                            <label for="autor">Autor:</label>
                            <input type="text" name="autor" id="autor" placeholder="autor" value="<%= itemToEdit.autor %> " required>
                        </div>
                        <div class="input">
                            <label for="isbn">ISBN:</label>
                            <input type="text" name="isbn" id="isbn" placeholder="isbn" value="<%= itemToEdit.isbn %>"  required>
                        </div>
                        <div class="input">
                            <label for="categoria">Categoria: (<%= itemToEdit.categoria  %>) </label>
                            <select name="categoria" id="categoria">
                                <option value="general">Categoria del Libro</option>
                                <% for( let index = 0; index < categorias.length; index++ ) { %>
                                    <option value="<%= categorias[index]  %>" ><%= categorias[index]  %></option>
                                <% } %>
                            </select>
                        </div>
                        <div class="input">
                            <label for="numero_paginas"> Número de Páginas:</label>
                            <input type="number" name="numero_paginas" id="numero_paginas" placeholder=" # Paginas" value=<%= itemToEdit.numero_paginas %> required>
                        </div>
                    </div>
                    <div class="segunda-seccion">
                        <div class="input">
                            <label for="formato">Formato: (<%= itemToEdit.formato  %>)</label>
                            <select name="formato" id="formato">
                                <option value="Male">Dirigido a:</option>
                                <% for( let index = 0; index < formatos.length; index++ ) { %>
                                    <option value="<%= formatos[index]  %>" ><%= formatos[index]  %></option>
                                <% } %>
                            </select>
                        </div>
                        <div class="input">
                            <label for="precio">Precio:</label>
                            <input type="number" name="precio" id="precio" placeholder=" Precio" value=<%= itemToEdit.precio %> required>
                        </div>
                        <div class="input">
                            <label for="peso"> Peso:</label>
                            <input type="number" name="peso" id="peso" placeholder=" Peso" value=<%= itemToEdit.peso %> required>
                        </div>
                        <div class="input">
                            <label for="idioma">Idioma:</label>
                            <input type="text" name="idioma" id="idioma" placeholder="idioma" value="<%= itemToEdit.idioma %> " required>
                        </div>
                    </div>
                    <div class="form-confirm">
                        <div class="input botones">
                            <input type="reset" value="Cancelar">
                            <input type="submit" value="Guardar Cambios">
                        </div>
                    </div>
                </form>
            </div>
        </main>

        <%- include('../partials/footer') %>
        <script src="../../js/index.js"></script>  
    </body>
</html>